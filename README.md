# Havn workspace recovery archive

Historical branches and unfinished source are retained as parent commits.
See RECOVERY-INDEX.json for original refs, exact commits and snapshots.

This branch intentionally has no runnable application or deployment configuration.
Do not merge it into a production branch. Restore a selected original commit instead.
